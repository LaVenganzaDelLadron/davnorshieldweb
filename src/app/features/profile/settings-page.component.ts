import { Component, signal } from '@angular/core';
import { PageHeaderComponent } from '../../shared/components/cyber-ui/cyber-ui.component';

@Component({selector:'app-settings-page',standalone:true,imports:[PageHeaderComponent],template:`<section class="page-enter mx-auto max-w-7xl"><app-page-header title="Settings" subtitle="Manage your account, safety preferences, and interface."/><div class="grid gap-5 lg:grid-cols-[220px_1fr]"><nav class="cyber-card h-fit p-2">@for(tab of tabs;track tab){<button (click)="active.set(tab)" [class]="active()===tab?'settings-nav selected':'settings-nav'">{{tab}}</button>}</nav><article class="cyber-card p-6"><h2 class="text-lg font-semibold text-white">{{active()}}</h2><p class="mt-1 text-sm text-slate-500">Control how DavnorShield supports your cyber safety.</p><div class="mt-6 divide-y divide-slate-800">@for(setting of settings;track setting.title){<div class="flex items-center justify-between gap-5 py-4"><div><h3 class="text-sm font-medium text-slate-200">{{setting.title}}</h3><p class="mt-1 text-xs text-slate-500">{{setting.copy}}</p></div><button (click)="setting.on=!setting.on" class="toggle" [class.on]="setting.on" [attr.aria-label]="setting.title"></button></div>}</div><button class="btn btn-primary mt-6">Save preferences</button></article></div></section>`})
export class SettingsPageComponent {tabs=['Account','Notifications','Privacy','Security','Appearance','Danger Zone'];active=signal(this.tabs[0]);settings=[{title:'Real-time threat alerts',copy:'Receive high-priority community safety alerts.',on:true},{title:'Weekly security brief',copy:'A concise summary delivered every Monday.',on:true},{title:'Anonymous contribution metrics',copy:'Help improve local trends without revealing identity.',on:false},{title:'Reduce motion',copy:'Use calmer transitions throughout the interface.',on:false}];}
