import { Component, signal } from '@angular/core';
import { PageHeaderComponent } from '../../shared/components/cyber-ui/cyber-ui.component';

@Component({selector:'app-scanner-page',standalone:true,imports:[PageHeaderComponent],template:`<section class="page-enter mx-auto max-w-7xl"><app-page-header title="AI Threat Scanner" subtitle="Analyze suspicious messages, links, QR codes, and screenshots privately."/><div class="grid gap-5 xl:grid-cols-[1.4fr_1fr]"><article class="cyber-card p-5"><div class="flex flex-wrap gap-2">@for(tab of tabs;track tab){<button (click)="active.set(tab)" [class]="active()===tab?'scan-tab selected':'scan-tab'">{{tab}}</button>}</div><label class="mt-6 block text-sm font-medium text-slate-200">Paste suspicious {{active().toLowerCase()}} content</label><textarea class="input mt-2 h-42 resize-none" placeholder="Paste a link, SMS, or describe what you received..."></textarea><div class="upload-zone mt-4">⇧<p class="mt-2 text-sm text-slate-300">Drop an image or QR screenshot here</p></div><button (click)="analysed.set(true)" class="btn btn-primary mt-5 w-full py-3">⌁ Analyze with DavnorShield AI</button></article><article class="cyber-card p-6">@if(analysed()){<span class="pill risk-high">DANGEROUS</span><div class="gauge mt-5"><b>86</b><span>THREAT SCORE</span></div><h3 class="mt-5 text-lg font-semibold text-white">GCash credential phishing</h3><p class="mt-2 text-sm text-slate-400">Lookalike payment domain and urgency language detected.</p><div class="mt-5 space-y-3"><p class="soft-row text-sm">⚠ Do not open or reply to the sender.</p><p class="soft-row text-sm">✓ Block the account and report it.</p></div>}@else{<div class="flex min-h-90 flex-col items-center justify-center text-center"><div class="scanner-ring">⌁</div><h3 class="mt-5 font-semibold text-white">Ready to scan</h3><p class="mt-2 max-w-xs text-sm text-slate-500">Get a risk score, detected indicators, and recommended next steps.</p></div>}</article></div></section>`})
export class ScannerPageComponent {tabs=['URL Scanner','SMS Scanner','QR Scanner','Screenshot Scanner'];active=signal(this.tabs[0]);analysed=signal(false);}
